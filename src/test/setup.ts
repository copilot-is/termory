// Vitest setup — runs once before every test file in the project.
// Registers `@testing-library/jest-dom` custom matchers
// (`toBeInTheDocument`, `toHaveClass`, etc.) onto Vitest's `expect`.
import "@testing-library/jest-dom/vitest";
